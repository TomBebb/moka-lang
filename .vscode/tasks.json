{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Opam",
            "type": "shell",
            "command": "eval $(opam env) && dune build src/main.exe",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}